<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.iiitmstars">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.aboutiiitm.VisionInfoActivity" />
        <activity android:name=".ui.aboutiiitm.FacilitiesActivity" />
        <activity android:name=".ui.aboutiiitm.MissionVisionActivity" />
        <activity android:name=".ui.aboutiiitm.PPPForIIITManipurActivity" />
        <activity android:name=".ui.aboutiiitm.AboutUsActivity" />
        <activity android:name=".ui.academics.AcademicsActivity" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>